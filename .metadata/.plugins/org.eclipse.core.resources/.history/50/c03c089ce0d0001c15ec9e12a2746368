package com.threads.reflextion;

import com.threads.Actor;

public class Metods {
	public static void main()
	{
		public static void testA()
		
		{
		String s1="Hello"; 
		printClassInfo (s1);
		Integer x=2000;
		printClassInfo (x);
		Emp firstEmp=new Emp();
		printClassInfo (first Emp);
		Actor hero=new Actor();
		void printClassInfo (hero );
	}

}
